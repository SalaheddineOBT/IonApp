import { FilterMarquesPipe } from './filter-marques.pipe';

describe('FilterMarquesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMarquesPipe();
    expect(pipe).toBeTruthy();
  });
});
